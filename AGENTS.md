# Project agents guide — Micocards (root)

The authoritative agent guide for this repo lives in `CLAUDE.md`. This file exists only as a pointer for tooling that defaults to `AGENTS.md`.

Read `CLAUDE.md` (root), `frontend/CLAUDE.md`, and `backend/CLAUDE.md` for the actual rules.
