/**
 * features/auth-form — shared building blocks for `pages/auth-login` and
 * `pages/auth-register`. Each export is used by both pages (the FSD pages-first
 * extraction trigger from CLAUDE.md).
 */
export { AuthCard } from './ui/AuthCard.tsx';
export { AuthTabs } from './ui/AuthTabs.tsx';
export { AuthField } from './ui/AuthField.tsx';
export { SubmitButton } from './ui/SubmitButton.tsx';
